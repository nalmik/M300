tce-load -wi kmaps
sudo loadkmap < /usr/share/kmap/qwertz/de_CH-latin1.kmap

sudo curl -L "https://github.com/docker/compose/releases/download/1.24.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose


WARNING: Image for service mpd was built because it did not already exist. To rebuild this image you must use `docker-compose build` or `docker-compose up --build`.



